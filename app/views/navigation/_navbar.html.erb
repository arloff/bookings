<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to 'Schwalbennest Arloff', root_path, class: "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <%= render "nav/item", show: (can? :index, Booking),
                            text: t('activerecord.views.booking.index'),
                            page: bookings_path %>
      <%= render "nav/item", show: (can? :create, Booking),
                            text: t('activerecord.models.booking'),
                            page: new_booking_path %>

      <li class="nav-item">
        <% if user_signed_in? %>
        <%= link_to t('devise.login.sign_out'), destroy_user_session_path, method: :get, class: 'nav-link' %>
              <% else %>
        <%= link_to t('devise.login.sign_in'), new_user_session_path, class: "nav-link" %>
        <% end %>
      </li>
    </ul>
  </div>
</nav>
